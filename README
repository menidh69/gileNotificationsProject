Requirements: 
Docker installed
Git installed
Node 20.15

# Clone repository and submodules
git clone --recurse-submodules 

# Init and update submodules if git clone wasn´t recursive
git submodule init
git submodule update

# Run project
To use Nest JS application run in terminal:
  `docker-compose -f docker.compose.yml build`
  `docker-compose -f docker.compose.yml up -d`

# Database access
Connect to database from localhost:
  Host: localhost
  User: root
  Password: admin
  Port: 3307
